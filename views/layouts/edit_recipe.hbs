<!DOCTYPE html>
<html lang="es" data-bs-theme="ligth">
<head>
    <meta charset="utf-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body> 
    <div class="shadow p-3 mb-5 bg-body-tertiary rounded">
        <div class="container mt-3">
            <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
                <div class="">
                    <a href="/" class="">
                        <svg class="bi" width="40" height="32" role="img" aria-label="Bootstrap"><use xlink:href="#bootstrap"></use></svg>
                    </a>
                </div>
                {{> barrita_de_arriba}}
            </header>
        </div>
        </div>
        
        <form action="/update_recipe" method="post">
    <input type="hidden" name="id" value="{{recipe.recetaid}}">
    <label for="nombre">Nombre de la receta</label>
    <input type="text" class="form-control" name="nombre" id="nombre" value="{{recipe.nombre}}" required="">
    <br>
    <label for="categoria">Categoría</label>
    <select class="form-control" name="categoria" id="categoria" required="">
        <option value="">Selecciona una categoría</option>
        {{#each categories}}
            <option value="{{this.categoriaid}}" {{#if (eq this.categoriaid recipe.categoria)}}selected{{/if}}>{{this.tipo}}</option>
        {{/each}}
    </select>
    <br>
    <label for="ingredientes">Ingredientes</label>
    <select class="form-control" name="ingredientes" id="ingredientes" required="">
        <option value="">Selecciona un ingrediente</option>
        {{#each ingredients}}
            <option value="{{this.ingredientesid}}" {{#if (eq this.ingredientesid recipe.ingredientes)}}selected{{/if}}>{{this.nombre}}</option>
        {{/each}}
    </select>
    <br>
    <label for="pasos">Pasos</label>
    <textarea class="form-control" name="pasos" id="pasos" rows="10" required="">{{recipe.pasos}}</textarea>
    <br>
    <label for="tiempo">Tiempo de preparación (en minutos)</label>
    <input type="number" class="form-control" name="tiempo" id="tiempo" min="1" value="{{recipe.tiempo}}" required="">
    <br>
    <input type="submit" class="form-control text-center nav-link py-2" style="background-color: aqua;" value="Actualizar receta">
</form>

<div class="container">
        <footer class="py-3 my-4">
          <p class="text-center text-body-secondary">© 2023 Company, Inc</p>
        </footer>
    </div>
        </body>
</html>
