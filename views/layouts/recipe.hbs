<!DOCTYPE html>
<html lang="es" data-bs-theme="ligth">
<head>
    <meta charset="utf-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link href="styles.css" rel="stylesheet">
</head>
<body> 
    <div class="shadow p-3 mb-5 bg-body-tertiary rounded">
        <div class="container mt-3">
            <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
                <div class="">
                    <a href="/" class="">
                        <svg class="bi" width="40" height="32" role="img" aria-label="Bootstrap"><use xlink:href="#bootstrap"></use></svg>
                    </a>
                </div>
                {{> barrita_de_arriba}}
            </header>
        </div>

        <div class="container">
            <h1 class="text-center">recetas</h1>
            <form action="/recipe" method="get">
            </form>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Nombre</th>
                        <th>Ingredientes</th>
                        <th>Categoría</th>
                        <th>Pasos</th>
                        <th>Tiempo</th>
                        <th>Favoritos</th>
                    </tr>
                    {{#each recipes}}
                    <tr>
                        <td>{{this.nombre}}</td>
                        <td>{{this.ingredientes}}</td>
                        <td>{{this.categoria}}</td>
                        <td>{{this.pasos}}</td>
                        <td>{{this.tiempo}} minutos</td>
                        <td>{{this.votos}}</td>
                        <td>
                    <a href="/favorite?recipeId={{this.recetaid}}" class="heart" onclick="this.classList.toggle('clicked');">
                        <i class="far fa-heart"></i> <!-- icono de corazón -->
                        </a>
                    </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>

        <div class="container">
            <footer class="py-3 my-4">
                <p class="text-center text-muted">© 2023 Company, Inc</p>
            </footer>
        </div>
    </div>

</body>
</html>