<!DOCTYPE html>
<html>
<head>
    <title>Register</title>
    <meta charset="utf-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body style="min-height: 100vh;">
    <div class="shadow p-3 mb-5 bg-body-tertiary rounded">
        <div class="container">
          <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
            <div class="col-md-3 mb-2 mb-md-0">
              <a href="/" class="d-inline-flex link-body-emphasis text-decoration-none">
                <svg class="bi" width="40" height="32" role="img" aria-label="Bootstrap"><use xlink:href="#bootstrap"></use></svg>
              </a>
            </div>
            {{> barrita_de_arriba}}
          </header>
        </div>
    </div>
  
    <div class="d-flex align-items-center justify-content-center py-4 text-emphasis-primary">
    <div class="shadow">
        <main class="border p-4 rounded bg-white ">
            <h2 class="h3 mb-3 fw-normal text-center" class="">Subir receta</h2>
            <div class="form-floating">
                <form action="/upload" method="post">
                    <label for="nombre">Nombre de la receta</label>
                    <input type="text" class="form-control" name="nombre" id="nombre" required="">
                    <br>
                    <label for="categoria">Categoría</label>
                    <select class="form-control" name="categoria" id="categoria" required="">
                        <option value="">Selecciona una categoría</option>
                        {{#each categories}}
                        <option value="{{this.categoriaid}}">{{this.tipo}}</option>
                        {{/each}}
                    </select>
                    <br>
                    <label for="ingredientes">Ingredientes</label>
                    <select class="form-control" name="ingredientes" id="ingredientes" required="">
                        <option value="">Selecciona un ingrediente</option>
                        {{#each ingredients}}
                        <option value="{{this.ingredientesid}}">{{this.nombre}}</option>
                        {{/each}}
                    </select>
                    <br>
                    <label for="pasos">Pasos</label>
                    <textarea class="form-control" name="pasos" id="pasos" rows="10" required=""></textarea>
                    <br>
                    <label for="tiempo">Tiempo de preparación (en minutos)</label>
                    <input type="number" class="form-control" name="tiempo" id="tiempo" min="1" required="">
                    <br>
                    <input type="submit" class="form-control text-center nav-link py-2" style="background-color: aqua;" value="Subir receta">
                </form>
            </div>
        </main>
    </div>
</div>  

    <div class="container">
        <footer class="py-3 my-4">
          <p class="text-center text-body-secondary">© 2023 Company, Inc</p>
        </footer>
    </div>
    
</body>
</html>